import CoreGraphics

/// Corner radii for the Felt & Foil design system.
enum AppRadius {
    static let chip: CGFloat = 999          // Pill / capsule shape
    static let chipPlaque: CGFloat = 4      // Rectangular high-denom chip
    static let card: CGFloat = 12
    static let sheet: CGFloat = 24
    static let badge: CGFloat = 6
}
