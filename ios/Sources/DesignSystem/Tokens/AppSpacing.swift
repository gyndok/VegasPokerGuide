import CoreGraphics

/// Spacing constants for the Felt & Foil design system.
enum AppSpacing {
    static let xs: CGFloat = 4
    static let s: CGFloat = 8
    static let m: CGFloat = 12
    static let l: CGFloat = 16
    static let xl: CGFloat = 24
    static let xxl: CGFloat = 32

    /// Row vertical padding inside list items.
    static let rowVerticalPadding: CGFloat = 8
    /// Card interior padding.
    static let cardInsets: CGFloat = 16
    /// Width of the venue color bar on the leading edge of EventRow.
    static let venueBarWidth: CGFloat = 4
}
